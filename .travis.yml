language: java
install: true
script: ./travis.sh
addons:
  sonarcloud:
    organization: bakdata
    token: $SONAR_TOKEN
    branches:
      - master
      - dev
    github_token: $SONAR_TOKEN

jdk:
- oraclejdk8

cache:
  directories:
  - $HOME/.m2
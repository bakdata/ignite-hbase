language: java
install: true
addons:
  sonarcloud:
    organization: bakdata
    token: $SONAR_TOKEN
    branches:
      - master
      - dev
script:
  - sonar-scanner

jdk:
- oraclejdk8

cache:
  directories:
  - $HOME/.m2